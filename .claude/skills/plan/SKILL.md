---
name: plan
description: >
  Phase를 기획한다. "기획해줘", "요구사항 정리", "Phase 계획",
  "뭐부터 해야 해", "우선순위" 등의 요청 시 사용한다.
  문제 검증(gstack) + User Story Mapping + INVEST/Given-When-Then 통합 방법론.
version: 3.1
triggers:
  - 기획
  - 요구사항
  - 요구 사항
  - 범위
  - Phase
  - 계획
  - 시작
  - 뭐부터
---

> **이 스킬은 project-agent가 실행합니다.**

## 디스패치

> **필수 선행**: 이 스킬을 실행하기 전에 `.claude/skills/_shared/execution-contract.md`(실행 계약)를 읽는다.
> 아래는 그 계약의 이 스킬용 요약이다. **상세 규칙과 금지 사항은 계약 문서가 기준이며, 충돌 시 계약이 우선한다.**

### 0단계: 재개 확인 (계약 §0)

가장 먼저 `.orchestrator/status/plan.json`을 읽는다.

- 없으면 생성하고 `current_step: 1`로 시작한다
- 있으면 `completed_steps`의 **다음 단계부터** 재개한다
- **`completed_steps`에 없는 단계를 수행했다고 가정하지 않는다**
- 각 단계 종료 시 즉시 갱신한다 (몰아서 기록 금지)

### 사전 점검 — 3단 검증 (계약 §1)

**L1 — 존재**

```bash
ls docs/feedback/ 2>/dev/null      # 이전 Phase OPS-005 (첫 Phase면 비어 있어도 정상)
git branch --show-current
git status --short
```

**L2 — 내용**

```bash
# 이전 Phase 피드백이 있다면 빈 껍데기가 아닌지
for f in docs/feedback/*.md; do [ -s "$f" ] || echo "EMPTY: $f"; done
# Notion 원본 링크 누락 탐지 (계약 §3 위반)
grep -L 'Notion' docs/feedback/*.md 2>/dev/null
```

**L3 — 정합**

첫 Phase는 대조 대상이 없다. 후속 Phase는 이전 Phase의 미완료 스토리가 이번 범위에 승계되었는지 확인한다.

**판정**: L1 실패 → operate 스킬 회귀 제안(진행 차단) · L2 실패 → 해당 문서 회귀(진행 차단) · L3 편차 → 대표 보고(차단 없음)

`ls`가 성공했다는 이유만으로 통과 처리하지 않는다.

### 범위 변경 트리거 확인 (계약 §5)

**이 스킬은 범위를 정하는 스킬이므로 트리거 판정의 1차 책임을 진다.**

| 지표 | 임계 |
|------|------|
| Must 스토리 개수 증감 | **≥ 20%** |
| 복잡도 L 항목 개수 | **≥ 2배** |
| DB 테이블 개수 증감 | ≥ 50% |
| 구현 Layer 개수 | +1 이상 |

하나라도 해당하면:

1. **즉시 대표에게 보고**한다 (의사결정 등급 **높음**)
2. PLN-002 Phase 계획서를 재산정한다
3. 한 Phase가 과대해지면 **Phase 분할을 선택지로 제시**한다 — Walking Skeleton의 이점("빠른 동작 확인")이 사라지기 때문이다

> 실제 사례: Phase 1 Must 12 → 19개(+58%), 복잡도 L 2 → 6개(3배). 트리거가 없어 사람이 수동 발견했다 (D-30).

### 위임

사전 점검 통과 시, project-agent를 생성한다:
- subagent_type: `project-agent`
- 전달: 스킬 `plan`, 경로 `.claude/skills/plan/SKILL.md`
- **함께 전달**: `.claude/skills/_shared/execution-contract.md` 경로와 "이 계약을 먼저 읽고 실행하라"는 지시
- Notion 요구사항이 있으면 내용을 프롬프트에 포함하여 전달

### 완료 후

project-agent 완료 시, **아래 검증을 통과하기 전에는 결과를 전달하지 않는다.**

**1. 결정 전파 잔여 확인 (계약 §2)**

승인·결정을 반영한 경우, 옛 표기가 남아 있지 않은지 전수 검색한다.

```bash
grep -rn "<바뀐 옛 표기>" docs/    # 출력이 비어야 통과
```

**"변경 예정" · "반영 대기" · "개정 대상" 상태로 종료하지 않는다.** 이 상태는 다음 세션에서 잊힌다.

**2. 이중 기록 검증 (계약 §3)**

```bash
find docs -name '*.md' ! -name 'CLAUDE.md' -exec grep -L 'Notion' {} +   # 출력이 비어야 통과
```

- Git `docs/` + Notion `02. 프로젝트 / ClaudeManager / 01. 기획` 양쪽에 존재
- 각 문서 헤더에 `> **원본**: [Notion {코드}](url) · Git 동기화 {날짜}`
- `docs/00-progress.md` 동기화 표 + Notion `00. 진행 상황` 갱신

**3. 보고 전 산출물 검증 (계약 §4)**

```bash
for f in <보고서 ## 산출물 칸의 경로 전건>; do
  [ -f "$f" ] && echo "OK   $f" || echo "MISSING $f"
done
```

`MISSING`이 하나라도 있으면 **보고를 중단하고** 누락을 채운다. 채울 수 없으면 산출물 칸에서 지우고 `## 미해결 사항`으로 옮긴다.

**4. 상태 파일 최종 갱신 (계약 §0)**

`.orchestrator/status/plan.json`에 `completed_steps` 전건과 `artifacts`(code / git / notion 3필드)를 기록한다.

**5. 결과 전달 및 전환**

1. 결과를 대표에게 전달
2. 다음 스킬 전환 정보에 따라:
   - 자동 → 해당 스킬 즉시 실행
   - 승인 필수 → 대표에게 실행 여부 확인

---

## 입력 (이전 스킬에서 받는 바통)

- 대표 요청 (신규 Phase)
- OPS-005 다음 Phase 피드백 (후속 Phase)

## 출력 (다음 스킬에 넘기는 바통)

- PLN-001 요구사항 정의서 (Story Map + 요구사항 목록 + 수용 기준)
- PLN-002 Phase 계획서 (Walking Skeleton + 작업 목록)
- PLN-003 우선순위 목록 (MoSCoW 분류)
- PLN-004 용어 사전
- PLN-005 프로젝트 코드 체계

## 필요 권한

- 도구: Read, Write, AskUserQuestion
- Sub-Agent: 없음

---

## 절차

### 1단계: 컨텍스트 수집

실행 전에 아래 자료를 읽고 현재 상태를 파악한다:

- `docs/requirements/` — 기존 요구사항 정의서
- `docs/feedback/` — 이전 Phase 운영 피드백 (OPS-005)
- `docs/00-progress.md` — 전체 진행 상황
- `.orchestrator/learnings.jsonl` — 프로젝트 학습 기록

이미 답이 있는 항목은 이후 단계에서 건너뛴다.

### 2단계: 문제 검증 — 강제 질문

계획을 세우기 **전에** 전제를 검증한다. 아래 4가지 강제 질문을 대표에게 던진다 (AskUserQuestion).
모호한 답변은 구체적으로 다시 묻는다 — "관심이 있다" 수준은 부족하고, 증거를 요구한다.

| 강제 질문 | 목적 | 모호할 때 후속 질문 |
|----------|------|-------------------|
| **수요 현실**: 이 기능이 실제로 필요하다는 증거는? (현재 어떤 고통이 있는가?) | 막연한 희망 vs 실제 고통 구분 | "구체적으로 언제, 어떤 상황에서 불편을 겪었나?" |
| **현재 대안**: 지금은 이 문제를 어떻게 해결하고 있으며, 그 비용(시간/노력)은? | 현재 대안의 고통 수준 측정 | "수동 대안으로도 충분하다면, 자동화할 가치가 있는가?" |
| **최소 범위**: 이 Phase에서 며칠 안에 동작 확인할 수 있는 최소 버전은? | 범위 과잉 방지 (Narrowest Wedge) | "이 기능 목록 중 절반을 빼야 한다면 뭘 남기나?" |
| **전제 도전**: 이 계획의 숨겨진 가정은 무엇이며, 그 가정이 틀렸다면? | 실패 시나리오 사전 식별 | "이것이 실패하는 시나리오를 하나 그려보면?" |

**후속 Phase인 경우**: OPS-005 피드백이 있으면 "운영에서 발견된 것 중 예상과 달랐던 점은?"을 추가한다.

**원칙**: 구체성만이 통화다. 카테고리("사용자"), 전략("확장성"), 희망("좋을 것 같다")은 답이 아니다.

### 3단계: 대안 탐색

검증된 문제에 대해 **2~3개 접근법**을 생성하고 비교한다.

```
### 접근법 A: {이름}
- **핵심**: (한 줄 설명)
- **범위**: (포함/제외 기능)
- **장점**: ...
- **리스크**: ...
- **예상 규모**: (스토리 수, 복잡도)

### 접근법 B: {이름}
- ...

### 접근법 C: {이름} (선택)
- ...

### 추천
- **추천안**: 접근법 {X}
- **근거**: (왜 이것이 최선인지)
- **기각 사유**: (다른 접근법을 선택하지 않는 이유)
```

대표에게 추천안을 제시하고 선택을 요청한다 (AskUserQuestion, 의사결정 등급: 높음).

### 4단계: User Story Mapping

선택된 접근법을 기반으로 **Activity → Task → Story** 계층으로 분해한다.

```
Activity (대분류 — 사용자의 큰 목표)
├── Task (중분류 — 목표 달성을 위한 단계)
│   ├── Story (소분류 — 구현 가능한 단위 기능)
│   └── Story
└── Task
    ├── Story
    └── Story
```

**예시**:
```
[에이전트 관리] ← Activity
├── [에이전트 생성] ← Task
│   ├── 대표로서 프로젝트 에이전트를 생성하여 작업을 위임한다 ← Story
│   └── 대표로서 에이전트 설정을 지정하여 역할을 제한한다 ← Story
└── [에이전트 모니터링] ← Task
    ├── 대표로서 에이전트 상태를 확인하여 진행을 파악한다 ← Story
    └── 대표로서 에이전트를 일시정지하여 비용을 제어한다 ← Story
```

### 5단계: 스토리 작성 — INVEST 기준

각 Story를 아래 형식으로 작성한다:

```
### [요구사항 코드] 스토리 제목

- **스토리**: [역할]로서 [기능]을 하여 [이유/가치]를 얻는다
- **분류**: FR | NFR | UIR | DAT | INT
- **우선순위**: Must | Should | Could | Won't (이번 Phase)

#### 수용 기준

- [ ] Given [사전 조건] When [행동] Then [기대 결과]
- [ ] Given ... When ... Then ...
```

**요구사항 코드 부여 규칙**:
- FR-001, FR-002... (기능 요구사항)
- NFR-001... (비기능: 성능, 보안, 가용성)
- UIR-001... (UI/UX)
- DAT-001... (데이터)
- INT-001... (인터페이스/연동)

**INVEST 검증 체크리스트** (스토리마다 확인):

| 기준 | 질문 | 미충족 시 |
|------|------|----------|
| **I**ndependent | 다른 스토리 없이 독립 구현 가능한가? | 의존 스토리와 병합 또는 순서 명시 |
| **N**egotiable | 구현 방법이 열려 있는가? | "~방식으로"를 제거하고 목적만 남김 |
| **V**aluable | 사용자에게 가치를 주는가? | 기술 태스크면 가치 스토리에 포함 |
| **E**stimable | 규모를 판단할 수 있는가? | 불확실하면 spike(탐색) 스토리로 분리 |
| **S**mall | 한 Phase 내에서 완료 가능한가? | 더 작은 스토리로 분할 |
| **T**estable | 수용 기준으로 통과/실패 판단 가능한가? | Given-When-Then 재작성 |

### 6단계: 우선순위 결정 — MoSCoW

Story Map에서 각 스토리를 세로축 우선순위로 배치한다:

| 등급 | 기준 | 행동 |
|------|------|------|
| **Must** | 이것 없으면 Phase가 무의미 | 반드시 포함 |
| **Should** | 중요하지만 우회 가능 | 가능하면 포함 |
| **Could** | 있으면 좋음 | 여유 시 포함 |
| **Won't** | 이번엔 안 함 | 명시적으로 제외, 다음 Phase 후보 |

**Walking Skeleton 식별**: Must 스토리 중에서도 모든 Activity를 관통하는 최소 흐름을 찾는다. 이것이 Phase의 핵심 구현 범위가 된다.

```
Activity A ──── Must Story ────┐
Activity B ──── Must Story ────┤ Walking Skeleton
Activity C ──── Must Story ────┘ (최소 동작 흐름)
```

### 7단계: Phase 범위 확정

1. Walking Skeleton = Phase의 Must 범위
2. Should 스토리 = 시간 여유에 따라 포함 여부 결정
3. Could/Won't = 명시적으로 제외 기록
4. **대표에게 범위 승인 요청** (의사결정 등급: 높음)

### 8단계: 다중 관점 자체 검토

Phase 계획서 초안을 작성한 후, 아래 3가지 관점에서 자체 검토한다.
각 관점에서 발견된 문제는 계획서에 반영하거나 미해결 사항으로 기록한다.

#### 0. 범위 변경 트리거 재판정 (계약 §5) ★필수

다중 관점 검토에 들어가기 **전에**, 이번 계획이 이전 버전 대비 트리거를 넘겼는지 수치로 확인한다.

| 지표 | 이전 | 이번 | 증감 | 임계 | 발동 |
|------|:---:|:---:|:---:|:---:|:---:|
| Must 스토리 개수 | | | | ≥ 20% | |
| 복잡도 L 항목 개수 | | | | ≥ 2배 | |
| DB 테이블 개수 | | | | ≥ 50% | |
| 구현 Layer 개수 | | | | +1 | |

하나라도 발동하면 **대표에게 등급 '높음'으로 즉시 보고**하고, 한 Phase가 과대해졌다면 **Phase 분할을 선택지로 제시**한다. 분할해도 최종 범위는 그대로이므로 기존 승인을 뒤집지 않는다.

> 이 표를 채우지 않고 8단계를 통과시키지 않는다. 2026-09-01에 Must 12 → 19개(+58%), 복잡도 L 2 → 6개(3배) 증가를 자동 감지하지 못해 사람이 수동 발견했다 (D-30).


#### CEO 관점 (제품 가치)

| 점검 항목 | 확인 |
|----------|------|
| 이 Phase가 끝나면 실제로 사용할 수 있는 것이 있는가? | |
| Walking Skeleton이 사용자에게 가치를 주는 최소 흐름인가? | |
| Won't에 넣은 것 중 실은 Must인 것이 있는가? | |
| 10점짜리 제품에 가까워지고 있는가, 아니면 옆길로 빠지는가? | |

#### 기술 관점 (실현 가능성)

| 점검 항목 | 확인 |
|----------|------|
| 숨겨진 기술적 복잡성이 있는가? (과소평가한 스토리) | |
| 스토리 간 순환 의존이 있는가? | |
| 기존 코드/시스템과 충돌할 부분이 있는가? | |
| spike가 필요한 불확실한 영역을 식별했는가? | |

#### UX 관점 (사용자 경험)

| 점검 항목 | 확인 |
|----------|------|
| 사용자 흐름에 빠진 화면이나 상태가 있는가? | |
| 에러 상황에서 사용자가 막히는 지점이 있는가? | |
| Walking Skeleton만으로도 자연스러운 흐름이 되는가? | |

**검토 결과 보고 형식**:
```
### 다중 관점 검토 결과

- CEO: {발견사항 또는 "이상 없음"}
- 기술: {발견사항 또는 "이상 없음"}
- UX: {발견사항 또는 "이상 없음"}

반영 사항: {계획에 반영한 내용}
미해결: {대표 판단이 필요한 항목}
```

### 9단계: Phase 계획서 작성

PLN-002에 포함할 내용:
- Phase 목표 (1-2문장)
- 문제 검증 결과 요약 (2단계의 강제 질문 답변 핵심)
- 선택한 접근법과 기각 사유 (3단계)
- Walking Skeleton 정의
- 작업 목록 (Story 단위, 의존 순서 포함)
- 스킬별 예상 산출물
- 다중 관점 검토 결과 (8단계)
- 미결정 사항 목록
- Phase 완료 조건

### 10단계: 산출물 정리 및 보고

PLN-001~005를 docs/에 저장하고 대표에게 최종 보고한다.

---

## 산출물 상세 형식

### PLN-001 요구사항 정의서

```markdown
# 요구사항 정의서 v{N}

## 문제 정의
(강제 질문 결과: 수요 현실, 현재 대안, 최소 범위, 전제와 리스크)

## 선택한 접근법
(접근법 비교 요약 + 선택 근거)

## Story Map

### [Activity 1]
#### [Task 1.1]
- FR-001: 스토리 ...
  - Given ... When ... Then ...
#### [Task 1.2]
- FR-002: ...

## 요구사항 요약표

| 코드 | 분류 | 스토리 | 우선순위 | INVEST |
|------|------|--------|----------|--------|
| FR-001 | FR | ... | Must | ✅ |

## 변경 이력
```

### PLN-003 우선순위 목록

```markdown
# 우선순위 목록

## Walking Skeleton (Must)
- FR-001, FR-003, UIR-001 ...

## Should
- FR-005, NFR-002 ...

## Could
- FR-008 ...

## Won't (이번 Phase 제외)
- FR-010 ... → Phase N+1 후보
```

---

## 의사결정 포인트

- 문제 검증 결과 "진행할 가치 없음" 판단 시: 대표에게 즉시 보고 (높음)
- 접근법 선택: 대표 승인 필수 (높음)
- Phase 범위 확정 (Walking Skeleton): 대표 승인 필수 (높음)
- 우선순위 결정 (MoSCoW 배치): 대표 승인 필수 (높음)
- Won't 항목 확정: 대표 확인 (보통)

## 체크포인트

- 문제 검증 + 대안 비교 완료 후 중간 보고 (방향 확정 전)
- Story Map 초안 완성 후 중간 보고 (Activity-Task 구조 + 스토리 목록)
- 다중 관점 검토 후 Phase 계획서 최종 보고

## 제약 사항

- Phase 계획은 반드시 대표 승인 후 실행 (의사결정 등급: 높음)
- 미결정 사항은 명시적으로 표시
- 이전 Phase 운영 피드백이 있으면 반드시 반영
- 기존 요구사항을 반복 작성하지 않고 버전/변경 이력으로 연결
- 모든 Must 스토리에 Given-When-Then 수용 기준 필수 (Should는 권장, Could는 선택)
- 스토리가 INVEST 기준을 통과하지 못하면 수정 후 진행
- 모호한 답변을 수용하지 않는다 — 구체적 증거와 사례를 요구한다

## 완료 조건

- 대표 승인
- 문제 검증 완료 (강제 질문 4가지 답변 확보)
- 대안 비교 및 접근법 선택 완료
- 모든 Must 스토리에 수용 기준(Given-When-Then) 포함
- INVEST 기준 검증 완료
- Walking Skeleton 식별 완료
- 다중 관점 자체 검토 완료

## 실패/에스컬레이션 조건

- 문제 검증에서 "현재 대안으로 충분" 판단 시 대표에게 보고 (진행 여부 확인)
- 요구사항 간 상충 발견 시 대표에게 보고
- 기술적 실현 불가 판단 시 대표에게 에스컬레이션
- Must 스토리가 INVEST-Small 기준 미충족 시 대표와 분할 논의

## 완료 후 액션

1. 완료 요약 (문제 검증 결과 + 선택한 접근법 + 산출물 목록 + Story Map 개요)
2. docs/00-progress.md 갱신
3. 스킬 전환: plan → analyze는 **승인 필수**

## 다음 스킬

- analyze
