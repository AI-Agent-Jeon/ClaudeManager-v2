---
name: deploy
description: >
  시스템을 배포한다. "배포해줘", "릴리스", "출시해줘" 등의 요청 시
  사용한다. 빌드, 환경 설정, 배포 실행, 스모크 테스트를 수행한다.
version: 1.0
triggers:
  - 배포
  - 릴리스
  - 출시
  - deploy
  - 빌드
---

> **이 스킬은 project-agent가 실행합니다.**

## 디스패치

### 사전 점검

1. TST 전체 통과, PR 머지 확인: `gh pr list --state merged --limit 5 2>/dev/null`
2. 현재 브랜치 확인: `git branch --show-current`

누락 산출물이 있으면 test 스킬 회귀를 제안한다.

### 위임

사전 점검 통과 시, project-agent를 생성한다:
- subagent_type: `project-agent`
- 전달: 스킬 `deploy`, 경로 `.claude/skills/deploy/SKILL.md`

### 완료 후

project-agent 완료 시:
1. 결과를 대표에게 전달
2. 다음 스킬 전환 정보에 따라:
   - 자동 → 해당 스킬 즉시 실행
   - 승인 필수 → 대표에게 실행 여부 확인

---

## 입력 (이전 스킬에서 받는 바통)

- TST 전체 통과 확인
- DEV-003 PR 머지 완료

## 출력 (다음 스킬에 넘기는 바통)

- DPL-001 배포 체크리스트
- DPL-002 환경 설정 가이드
- DPL-003 릴리스 노트
- DPL-004 롤백 계획

## 필요 권한

- 도구: Read, Write, Bash (빌드/배포)
- Sub-Agent: 없음

## 절차

1. 배포 전 확인: 테스트 결과 리포트에서 치명/높음 결함 0건 확인
2. 배포 체크리스트 작성: 환경 변수, 의존성, 마이그레이션 확인 (자동)
3. 빌드 실행 (자동)
4. 사전 검증: 스모크 테스트 (자동)
5. 대표에게 배포 승인 요청 (실제 환경 변경 직전)
6. 배포 실행
7. 배포 후 모니터링
8. 릴리스 노트 작성
9. 배포 완료 보고

## 의사결정 포인트

- 실제 환경 배포 실행: 대표 승인 필수 (높음)
- 롤백 실행: 긴급 시 자동, 그 외 대표 승인

## 체크포인트

- 빌드 성공 후
- 스모크 테스트 통과 후 (배포 승인 요청 전)

## 제약 사항

- 배포 준비/체크리스트/빌드는 자동 수행
- 실제 환경 배포 실행만 대표 승인 필수 (의사결정 등급: 높음)
- 테스트 미통과 상태에서 배포하지 않는다
- 롤백 계획을 배포 전에 준비한다

## 완료 조건

- 대표 승인 후 배포 실행 완료
- 스모크 테스트 통과

## 실패/에스컬레이션 조건

- 빌드 실패 시 develop 스킬로 회귀
- 배포 후 장애 발생 시 롤백 실행 + 대표 보고

## 완료 후 액션

1. 완료 요약 (배포 결과 + 릴리스 노트)
2. docs/00-progress.md 갱신
3. 스킬 전환: deploy → operate는 **자동**

## 다음 스킬

- operate
